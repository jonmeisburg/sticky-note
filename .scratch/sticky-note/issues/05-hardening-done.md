# 05: Hardening + definition of done

**What to build:** Close out v1 by hardening the edges the happy path doesn't hit, and verifying the whole product against the spec's definition of done. Specifically: a saved position or size that falls off-screen (monitor change, resolution change) is clamped back on-screen at load so the note can never strand itself; a session-stability pass confirms the note runs without crashing or leaking over long uptime; and the full acceptance checklist is walked through manually, start to finish.

**Blocked by:** 01 (The note exists), 02 (Editing + autosave), 03 (Move by drag), 04 (Resize by corner).

**Status:** done (all boxes verified live 2026-09-01; recipe in `verify-live.md`)

**Known gap (2026-08-31, recorded not fixed):** the window is created before the async state load completes, so spawn sizing (`NoteWindow`'s `Component.onCompleted`) always reads defaults — the persisted doc size never applies at spawn. Inert in practice while the note auto-tiles (the compositor reshapes it at map anyway, and the post-load re-sync records what it settled on), but it means the doc's width/height record the last session's observed size, not the next session's spawn size. A fix would have to apply the doc size only when no compositor reshape has happened yet — otherwise it would fight tiling.

- [x] **Startup race (fixed 2026-08-31):** the spawn→tile geometry burst could arrive before the model's async file read and flush defaults over a note the model had never read — a data-loss path, found live. Fixed in the model (`NoteStateModel.qml`): nothing is written before the first read completes, and the first read adopts the disk document unconditionally; the view holds geometry syncs until the read lands and re-syncs once after. Regression-tested in `tests-harness.qml` ("a save before the first read cannot clobber the file").
- [x] **Superseded by the real-window amendment (closed 2026-09-01):** the compositor owns placement, so on-screen stranding is no longer our failure mode and x/y clamping was deleted from the codebase in the review cleanup (d0b83b9). Only the size floor survives — enforced by the window's native `minimumSize`, already covered by ticket 04's floor check.
- [x] After monitor change / resolution change, the note is still visible and usable after its next load. *(live-verified: shrank the note's home monitor DP-2 from 2560×1440 to 1920×1080@60 via `hl.monitor`; the note re-tiled fully inside the shrunken monitor (mapped + visible — under the real-window amendment placement is compositor-owned, so "still visible" is the compositor's guarantee, not a clamp). Then `omarchy restart shell` (the "next load"): the note re-mapped on its own with its text byte-identical; a click entered editing and a typed marker reached the state file through the debounced autosave. Monitor config restored to 2560×1440 and the note's text restored byte-exact. Backed at the seam by a new node test in `tests/NoteStateLogic.test.mjs`: an absurd-but-well-typed size in the document is accepted — the load path never rejects or clamps a recorded size, because it never applies it to the window either (the document is a record, not a driver).)*
- [x] Extended-use stability pass: no crash, no resource growth, no data loss across a long session with the note idle, edited, moved, and resized repeatedly. *(live-verified: 8 in-session churn cycles on the live shell (tile swap / workspace round-trip → float → absolute resize → unfloat → tiling reshape → edit → idle), then 5 `omarchy restart shell` cycles. Oracles per `verify-live.md`: shell RSS bounded 701.4→702.6 MB across all cycles (no growth trend), process alive at every sample (no crash), and zero data loss — the state document's text was asserted byte-exact after every cycle and every restart. Backed at seam 1 by a new harness step in `tests-harness.qml` ("churn: an edit burst and repeated saves lose nothing, watcher survives"): a 50-edit burst faster than the debounce settles to its last edit on disk, and after 8 repeated self-writes the file watcher still adopts an external edit — the dead-watch leak class this box guards against.)*
- [x] Full definition-of-done checklist from the spec passes, walked through manually and recorded in this ticket: appears on login with saved text/place/size; click to type with continuous autosave; corner-drag resize; drag to move; scrolls when full; sits behind windows; never steals focus unprompted. *(live-verified item by item, 2026-09-01:
  - **appears on login with saved text/place/size** — the five restart cycles above: the note re-maps on its own after each `omarchy restart shell` (the documented autostart path; a full reboot re-launches the same service), text byte-identical every time, and the recorded size round-trips (tiling may settle a different size, which the post-load re-sync records back — the "place is compositor-owned, size persists" semantics of the real-window amendment, as ticket 04's box allows).
  - **click to type with continuous autosave** — the box-1 demonstration (click → edit → typed marker on disk within the debounce).
  - **corner-drag resize / drag to move** — the native-resize and native-move demonstrations are ticket 04's and ticket 03's live records (both ticked); the stability pass above re-exercised both every cycle (float + absolute resize in both dimensions; tile swap + workspace move) with the text untouched.
  - **scrolls when full** — seeded a 91-line document into the live note; wheel-down (ydotool wheel, cursor parked on the note) scrolled the idle Flickable from its top (lines 01–86 visible, the end below the fold) to the bottom (lines 06–90 + `SN5-END-90` visible): the end of a long note is reachable, lines word-wrap with no horizontal scrollbar, no text clipped.
  - **sits behind windows** — *superseded by the real-window amendment* (user story 3 is on the amendment's superseded list: a real window tiles alongside the user's windows, not behind them). Recorded here so the pre-amendment item is explicitly closed, not silently dropped.
  - **never steals focus unprompted** — live check: with the terminal focused, an external text write (adopted by the model's watcher) plus a compositor resize on the note left the terminal the active window — the note never requests focus on its own (its only focus paths are the user's click and the compositor's focus rules; `NoteWindow` has no spawn-time focus request, and the `no_focus` window rule applies only to XWayland clients, not to the note). Note: after a full shell restart the compositor focuses the re-mapped note as any newly-mapped ordinary window would — standard compositor behavior for a real window (the amendment's explicit trade), not a self-request; in live use (no remap) the note never takes focus.
  - **never crashes or loses data over a week of real use** — the spec's standing DoD clause (spec:103). Same-day stand-in: box-2's churn + restart pass above (no crash, no growth trend, no data loss across ~40 state transitions and 5 remaps). The week clause itself is closed by the user's real use over time; it is recorded here as standing, not demonstrated — box-5's "full checklist passes" claim is scoped accordingly.)*